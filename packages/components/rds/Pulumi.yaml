name: rds-component
description: Infrastructure for the RDS database component in the monorepo
main: src/index.ts
runtime:
  name: nodejs
  options:
    packagemanager: npm
template:
  config:
    aws:region:
      description: The AWS region to deploy into.
      default: us-west-2