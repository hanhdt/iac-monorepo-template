{
  "name": "iac-monorepo-template",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "infra:init:dev": "lerna run infra:init:dev",
    "infra:deploy:dev": "lerna run infra:deploy:dev",
    "infra:destroy:dev": "lerna run infra:destroy:dev"
  },
  "workspaces": [
    "packages/*",
    "packages/components/*",
    "packages/functions/*",
    "infra"
  ],
  "devDependencies": {
    "@tsconfig/node20": "^20.1.4",
    "@types/node": "^22.7.5",
    "lerna": "^8.1.8",
    "typescript": "^5.6.3"
  }
}
